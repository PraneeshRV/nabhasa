ROLE: builder(UI) — nabhasa A2 slice P3b (approach-triggered portfolio panels). Headless. Bash DISABLED: never run tsc/tests/git — conductor runs gates. Read/Edit/Write only.

WORKTREE: /home/praneesh/Praneesh/Portfolio/nabhasa-p3a (branch a2-p3b — yes, the folder says p3a, ignore that; you are P3b). Paths relative to it.

READ FIRST:
1. docs/a2-fantasy-plan.md — section "P3 — Content docking" in full: interfaces for approachStore + ApproachPanel are BINDING.
2. src/world/LichPlanets.tsx — the exports you consume: nearestContentWorld(pos), APPROACH_RADIUS (=60), getPlanetPositions().
3. src/content/portfolio.json — the five sections (already merged; single copy source).
4. src/hud/Telemetry.tsx + src/hud/hudStore.ts — the DOM-overlay + throttled zustand idiom you must mirror.
5. docs/art-direction.md — typography table (Space Grotesk display / Inter body / JetBrains Mono data) + reduced-motion rules.
6. src/flight/ (how craft position is exposed — find the same source hudStore's sampler uses; reuse that idiom).

SCOPE (entire write set):
- NEW src/hud/approachStore.ts — zustand store per plan interface: { open, slot, world, set }. Throttled writes (~5Hz) from the sampler; mirror hudStore idiom.
- NEW src/hud/ApproachPanel.tsx — DOM overlay panel (NOT canvas): renders the portfolio.json section for the open slot. Slide-in 300ms (GSAP if already a dep — check package.json via Read; otherwise CSS transition); prefers-reduced-motion → instant. aria-live="polite" on panel title. Dismiss: ESC key or flying out of radius. Never blocks flight input: no pointer capture over the canvas; panel is dismissable, never modal.
- App.tsx — TWO small edits ONLY: (a) mount a sampler leaf inside the Canvas scene (a null-rendering component you define in ApproachPanel.tsx or approachStore.ts that useFrame-samples craft position at ~5Hz, calls nearestContentWorld, sets store when dist < APPROACH_RADIUS, clears when it leaves); (b) render <ApproachPanel/> in the DOM overlay layer next to where Telemetry mounts. Touch nothing else in App.tsx.

NON-SCOPE: LichPlanets.tsx, portfolio.json (read-only), Telemetry.tsx, courier.ts, shaders, physics. Do NOT add deps. Do NOT change APPROACH_RADIUS or OFFER_RADIUS (concentric 60/40 is the accepted default — panel opens outer ring, mission offers inner).

INVARIANT: panels are DOM (crisp, a11y); approach proximity-triggered, never forced; content JSON single source of copy; free flight never locked; static tier still shows panels (DOM works without webgl — sampler must no-op gracefully if positions are absent; guard for null).

BEHAVIOR (plan-verbatim): approach within 60 wu of a content world → panel slides in showing that world's section; ESC or fly-away dismisses; Brace/Riven/Corona (no slot) never open.

EXPECTED FAILURE MODES: craft position source may differ from what you expect — find how hudStore samples it and copy that exact pattern; if there is genuinely no way to read craft position without editing a non-scope file → Write .a2-blocked explaining, stop.

ABORT: any needed change outside write set → .a2-blocked at worktree root, stop.

DONE: Write .a2-done at worktree root: files created/edited, App.tsx lines touched, sampler mechanism, a11y notes, residuals. No test-pass claims.
