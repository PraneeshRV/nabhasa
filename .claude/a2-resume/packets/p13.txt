ROLE: builder(flight) — nabhasa A2 slice P1.3. Headless. Bash DISABLED: never run tsc/tests/git — conductor runs all gates. Read/Edit/Write only.

WORKTREE: /home/praneesh/Praneesh/Portfolio/nabhasa-p13 (branch a2-p13). Paths relative to it.

READ FIRST:
1. docs/a2-fantasy-plan.md — "The Reach world contract" table (masses, radii, orbits for all 8) and section P1, sub-slice P1.3 + the gravity.ts interface block.
2. src/world/planets.ts — REACH_SYSTEM (the mass/radius source of truth). Do not modify it.
3. src/flight/gravity.ts — current implementation and its three-free duplication pattern.
4. tests/gravity.test.ts — current tests; the existing orbit test must remain EXACT (signature unchanged).

SCOPE (entire write set):
- src/flight/gravity.ts — widen PLANET_MASS_EARTHS and PLANET_RADII_WU to 8 entries (values duplicated from the contract table: masses 0.4, 1.0, 0.8, 1.2, 1.1, 0.6, 95, 0.1; soft radii from the table radii — Threshold use ~6). Update the comment orbit list to 8 entries (130/260/400/560/1250/1600/2050/2700). Function signature gravityAccelWithPlanets(pos, out, positions, gms, softRadii) UNCHANGED. Keep the file three-free (no three.js imports) exactly as it is now — follow the existing duplication pattern, do NOT import from planets.ts if the current file doesn't.
- tests/gravity.test.ts — ADD an 8-body perturbation case: with 8 planet positions at the contract orbit radii, acceleration near a massive close world (e.g. Kiln at 560) is dominated by that world's term vs the others; Corona (mass 95 but at 2050) contributes negligibly at inner orbits (assert its term < some small fraction of nearest-world term — honest per plan). Do NOT weaken or modify existing tests; only add.

NON-SCOPE: planets.ts, LichPlanets.tsx, courier.ts, HUD, shaders, any other test. If arrays are consumed somewhere expecting length 3 (grep-by-Read callers of PLANET_MASS_EARTHS / PLANET_RADII_WU), note it as residual in .a2-done — do not edit other files.

INVARIANT: existing gravity/orbit tests stay green conceptually (you cannot run them — do not change their expectations); signature unchanged; PLANET_GM_SCALE mechanism untouched.

ABORT: needed change outside write set → Write .a2-blocked at worktree root with the reason, stop.

DONE: Write .a2-done at worktree root: files changed, the 8 mass/radius values used, residuals. Do not claim tests pass.
