ROLE: builder(tests) — nabhasa A2 test-tightening slice (proof-critic follow-up). Headless. Bash DISABLED: you cannot run vitest — conductor gates. Read/Edit/Write only.

WORKTREE: /home/praneesh/Praneesh/Portfolio/nabhasa-p13 (branch a2-tests). Paths relative to it.

READ FIRST: docs/a2-fantasy-plan.md contract table; src/world/planets.ts (REACH_SYSTEM); src/flight/gravity.ts (PLANET_MASS_EARTHS, PLANET_RADII_WU, PLANET_GMS); src/game/courier.ts (MISSIONS, waypoints); src/content/portfolio.json; tests/planets.test.ts + tests/gravity.test.ts + tests/courier.test.ts (current state — you APPEND/tighten, never weaken or delete existing assertions).

SCOPE (entire write set): tests/planets.test.ts, tests/gravity.test.ts, tests/courier.test.ts. NOTHING else — no src/ edits, no new test files.

WORK (from the proof-critic verdict, priority order):
1. tests/planets.test.ts — PIN the contract exactly: per-world orbitWu toBe(130/260/400/560/1250/1600/2050/2700), massEarthsTuned toBe(0.4/1.0/0.8/1.2/1.1/0.6/95/0.1), radiusWu per table. Tighten Kepler tolerance to 5% for the inner four worlds (Brace/Praesidium/Aletheia/Kiln), keep 30% for outer three.
2. tests/gravity.test.ts — cross-copy consistency block: import REACH_SYSTEM from planets.ts (planets.ts already loads fine under vitest — planets.test.ts proves it) and assert for all i: PLANET_MASS_EARTHS[i] === REACH_SYSTEM[i].massEarthsTuned; PLANET_RADII_WU[i] === REACH_SYSTEM[i].radiusWu EXCEPT Threshold index 7 (station soft radius 6.0 vs spec radius — read actual values and assert what IS the intended relation, with a comment). Plus toHaveLength(8) on PLANET_GMS and PLANET_RADII_WU. Plus exact toEqual on the full mass array.
3. tests/courier.test.ts — waypoint radii vs contract: for each mission destination, Math.hypot of its waypoint ≈ the REACH_SYSTEM orbitWu of the named world (import REACH_SYSTEM; tolerance ±1). Missions count toBe(5), ids pinned.
4. tests/planets.test.ts (or courier) — portfolio binding cross-check: import src/content/portfolio.json and assert for each of the 5 slots: section.world === the REACH_SYSTEM world whose contentSlot === slot; section.slot === key; myth non-empty.

DO NOT: import LichPlanets.tsx / Aurora.tsx / JumpGate.tsx / any .tsx (JSX + R3F may not load under the node test env — out of this slice); touch vitest config; weaken any existing assertion or tolerance; add deps.

INVARIANT: append-only tightening; every new assertion must be derivable from the plan contract table or current intended values — if code and table disagree, do NOT change the test to match code: Write .a2-blocked describing the mismatch and stop (that is a real finding, not a test bug).

DONE: Write .a2-done at worktree root: which assertions added per file, any table-vs-code discrepancies found. No pass claims.
