ROLE: builder(visual) — nabhasa A2 slice P5b (Threshold jump-gate). Headless. Bash DISABLED: never run tsc/build/git — conductor runs gates. Read/Edit/Write only.

WORKTREE: /home/praneesh/Praneesh/Portfolio/nabhasa-p5a (branch a2-p5b — folder name says p5a, ignore; you are P5b). Paths relative to it.

READ FIRST:
1. docs/a2-fantasy-plan.md — section "P5 — Eye-candy", JumpGate part + tier-behavior table. BINDING.
2. src/world/LichPlanets.tsx — the Threshold station structure from P1.2 (find the gate-ring placeholder geometry, its position source = getPlanetPositions()[7]) and where props/components mount.
3. src/world/Aurora.tsx + src/shaders/aurora.ts — the P5a idiom for a world-tracking additive TSL effect (position copy in useFrame, tier gating, dispose). Mirror it.
4. docs/art-direction.md — one hot accent --star-hot; bloom >1 emissive-only; gate is a SANCTIONED emissive surface.
5. src/core/quality.ts — tier keys idiom (auroraBands precedent).

SCOPE (entire write set):
- NEW src/world/JumpGate.tsx — Threshold's gate: thin emissive --star-hot ring aligned with the station's gate-ring placeholder + a subtle localized radial distortion (TSL screen-UV warp, cheap; NOT the lensing graph; gated to the gate's screen bounds). Tracks Threshold via getPlanetPositions()[7] (Aurora idiom). Tier behavior (plan-verbatim): distortion pass ON only for webgpu-high/webgpu-low; webgl2 = ring geometry only, distortion dropped; static = render null. If you add a quality key, additive only.
- src/world/LichPlanets.tsx — MINIMAL mount edit only: render <JumpGate tier=...> with/near the Threshold structure (LichPlanets self-probes tier — pass what it has; read how it resolves tier internally). Do not restructure anything else in the file.

NON-SCOPE: App.tsx, Aurora files (read-only), nebula, lensing graph, star, beams, swarm, planets.ts, any test, any HUD file. No new deps.

INVARIANT: TSL only ('three/webgpu'/'three/tsl', never bare 'three' for shader classes); ring emissive may exceed 1 (sanctioned — "what bloom is for"); distortion subtle and localized (must not touch the star or full screen); no setState in useFrame; dispose on unmount; static renders nothing.

EXPECTED FAILURE MODES: a true screen-space distortion pass may not be reachable without touching the post/lensing graph (non-scope). FALLBACK (allowed, note it): fake the fold in-world — e.g. a camera-facing disc inside the ring whose TSL material refracts/warps the backdrop (viewportSharedTexture or a cheap normal-offset shimmer), threshold-gated to the ring interior. Choose whichever stays in-scope; document the choice in .a2-done.

ABORT: needed change outside write set → .a2-blocked at worktree root, stop.

DONE: Write .a2-done at worktree root: files, approach chosen (real screen pass vs in-world fallback), tier matrix, mount line, residuals. No compile claims.
