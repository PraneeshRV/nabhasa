ROLE: builder(fixes) — nabhasa A2 critic-fix slice. 4 verified findings, conductor already decided each fix. Headless. Bash DISABLED — conductor runs gates. Read/Edit/Write only. Implement EXACTLY these fixes, nothing else.

WORKTREE: /home/praneesh/Praneesh/Portfolio/nabhasa-p4 (branch a2-fixes). Paths relative to it. READ each target file fully before editing.

FIX 1 (CRITICAL — finale Contact panel unreachable):
Problem: m5-gate's destination waypoint W_THRESHOLD is a static beacon; the live Threshold orbits (and courier shell() uses +sin z while live positions use −sin z), so after delivering m5 the player is thousands of wu from the live station and the Contact panel (proximity-triggered) never opens. The plan requires: finale = Contact panel opens (diegetic CTA).
Fix: in src/hud/MissionResult.tsx (read it first — it observes courier state to show the end-of-mission card): when the result being shown is a successful delivery of mission id 'm5-gate', also call useApproachStore.getState().set({ open: true, slot: 'Contact', world: 'Threshold' }) — exactly once per result show (guard with a ref or effect dependency so it doesn't re-fire every render). Import the store + Slot type from '../hud/approachStore' (adjust relative path to actual location). Do NOT touch src/game/courier.ts (FSM stays verbatim; tests pin it).
Add one comment line: the finale panel is event-driven because the live Threshold drifts from the fixed beacon.

FIX 2 (ESC dismiss non-sticky):
Problem: ApproachPanel ESC closes the panel, but ApproachSampler re-opens it ≤200ms later while still inside APPROACH_RADIUS.
Fix in src/hud/approachStore.ts: add `dismissed: string | null` to the store state (the world name the user ESC-dismissed). Sampler logic: when near && near.dist < APPROACH_RADIUS: if store.dismissed === near.name → do NOT open; else open as today. When the near world leaves radius (near falls null or dist >= APPROACH_RADIUS): clear dismissed (set to null) along with the existing close behavior.
Fix in src/hud/ApproachPanel.tsx: the ESC handler sets { open:false, slot:null, world:null, dismissed: <the world that was open> }.

FIX 3 (Kiln tether detaches from ring):
Problem: in src/world/LichPlanets.tsx the Tether co-rotates inside SphereBody's spinRef group, but the Ring is a static sibling — the tether anchor sweeps away from the ring it should connect to.
Fix: co-rotate Kiln's ring with the same axial spin so ring+tether stay attached: when a world has BOTH props.ring AND props.tether (only Kiln), render <Ring spec={spec}/> INSIDE the spinRef group (next to the Tether); worlds with ring but no tether (Corona) keep the current static Ring mount. Keep it minimal — a conditional at the two mount sites, no restructuring.

FIX 4 (panel content non-interactive):
Problem: src/hud/approach.css sets pointer-events:none on the root; nothing restores interactivity on the panel itself — text unselectable, future Contact links unclickable.
Fix: add `pointer-events: auto;` on the panel element's class (the panel box, not the root overlay). One line + keep the existing comment intact.

FIX 5 (double tier probe + wrong-tier transient):
Problem: LichPlanets self-probes detectTier() with useState default 'webgpu-high' — a lower-tier device renders full debris/antenna counts (and JumpGate's fold disc on webgl2) for the frames until the async probe resolves. App already resolved the tier. Also the file's header comment claims detectTier is "cached" — it is not (src/core/tiers.ts has no cache).
Fix: give LichPlanets an optional prop `tier?: Tier`; when provided, use it directly (no probe, no transient); keep the self-probe ONLY as fallback when the prop is absent — but change its useState default from 'webgpu-high' to null and render count-scaled props only after tier is known (multiplier 0 until resolved — conservative, no pop-in of wrong counts). Fix the stale header comment. In src/App.tsx change `<LichPlanets />` to `<LichPlanets tier={tier} />` (MainExperience has `tier` in scope) — touch nothing else in App.tsx. If JumpGate receives tier from LichPlanets' internal state, make sure it now receives the resolved value (prop-first).

SCOPE (entire write set): src/hud/MissionResult.tsx, src/hud/approachStore.ts, src/hud/ApproachPanel.tsx, src/hud/approach.css, src/world/LichPlanets.tsx, src/App.tsx (one-line prop only).
NON-SCOPE: courier.ts, gravity.ts, planets.ts, tests (a parallel slice owns tests/), JumpGate.tsx internals, Aurora, shaders, quality.ts. No new deps.

INVARIANT: FSM/scoring verbatim; no setState in useFrame; store writes only on change (keep the existing guard pattern); panel never modal; existing behavior preserved except the five fixes.

ABORT: any fix impossible without touching non-scope → Write .a2-blocked with the reason, stop.

DONE: Write .a2-done at worktree root: per-fix file+lines summary, residuals. No pass claims.
