# Nabhasa (नभस्) Implementation Plan — flagship neutron-star experience

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **Orchestration:** post-Fable, tasks are dispatched as GLM-5.2 delegation packets per `fable-orchestration-laws` — each task header carries the packet (role, scope, non-scope, invariant, gate). One merge gate, serialized. Conductor verifies every gate personally.

**Goal:** A navigable neutron-star system you pilot a craft through — flying IS the interaction and IS the mini-game (Bruno-Simon "site is the game"). Awwwards-submission quality is the DONE bar.

**Architecture:** Single R3F `<Canvas>` running three r185 `WebGPURenderer` (auto WebGL2 fallback), one TSL shader codebase compiled to WGSL+GLSL. Rapier rigid body for the craft with hand-applied inverse-square gravity; free-flight camera rig; four signature set-pieces discovered spatially (no scroll choreography). Diegetic real-physics HUD; WebAudio ambient bed + live star-sonification layer. Four quality tiers, mobile = designed-down linear film, reduced-motion = static.

**Tech Stack:** Vite + React 19 + TypeScript · three r185 (`three/webgpu`, `three/tsl`) · @react-three/fiber v9.6+ · @react-three/drei v10 · @react-three/rapier v2.2.0 (lazy) · zustand v5 · GSAP 3.13 (scripted beats only — no ScrollTrigger, no Lenis: there is no scroll) · raw WebAudio · vitest.

**Source docs (vault):** `Dying-Star/Final Plan (locked 2026-07-05).md` (decisions), `Dying-Star/Research/*` (verified facts, patterns, pipeline). Conflicts resolve to Final Plan, then `Verified Tech Facts 2026-07`.

## Global Constraints

- **Repo:** `~/Praneesh/Portfolio/nabhasa`. Fresh. Deploy target: Vercel static (Vite build).
- **Renderer:** `WebGPURenderer` from `three/webgpu`; ALL shaders in TSL from `three/tsl`. Never import shader-relevant classes from bare `three`. No `@react-three/postprocessing` (WebGL-only) — post = native three `RenderPipeline`/`PostProcessing` TSL nodes.
- **Quality tiers (locked):** `webgpu-high` / `webgpu-low` / `webgl2` / `static`. Every scene task must state behavior per tier.
- **FPS gate (every visual task):** ≥60fps mid-tier laptop (dev machine, DPR 1.5, 1080p) on its tier; ≥30fps webgl2 tier. Measured with the `PerfProbe` from Task 2 — paste probe output in task report.
- **Asset budget:** initial payload ≤8MB (target 5), JS ≤500KB gzip ex-Rapier, Rapier WASM (~300KB gz) lazy-loaded, textures KTX2, fonts woff2 subset ≤120KB total, audio synth-first (≤1MB files).
- **Perf rules:** never `setState` in `useFrame`; refs + throttled zustand sink (10Hz). Clamp `dt ≤ 1/30`. Fixed-step accumulator 60Hz for physics. Dispose everything on unmount.
- **Anti-slop constitution (binding, from Plan B):** art-direction doc BEFORE scene code (Task 0 gates all visual tasks); no default bloom-on-everything (post designed per-region); custom type + physics-accurate copy (Praneesh reviews); designed sound; crafted loader/cursor/404; mobile designed-down, never broken; every scene passes "would Lusion ship this?" review.
- **Fonts:** Space Grotesk (display) + Inter (body) + JetBrains Mono (telemetry). OFL, self-hosted, latin subset. NO Orbitron (slop tell).
- **Tone mapping:** `AgXToneMapping`, output `SRGBColorSpace`. Exposure tuned per region, committed as constants.
- **Physics display honesty:** sim uses gameplay-tuned constants (`world/scale.ts`); HUD shows REAL astrophysics computed from real constants (`hud/physics-data.ts`). Never mix the two. Star modeled on **PSR B1257+12 "Lich"** (P = 6.219 ms → spin ~160.8 Hz, M = 1.4 M☉, R = 10 km; planets Draugr/Poltergeist/Phobetor real data — [VERIFY] all values at physics review). Decided 2026-07-06 per Amendment A1 (supersedes B0329+54). Visual beam sweep runs 1:2000 slowed (photosensitivity: keeps flash events < 3Hz); HUD labels "display rate 1:2000". Sonification uses the real ~161Hz spin tone.
- **Licensing:** NASA/STScI = public domain (strip insignia); ESA = CC BY (credit in footer); Freesound CC0 only; AI gen = moodboard only, NEVER shipped. Per-asset license line in `docs/assets-ledger.md`.
- **Commits:** conventional, no Co-Authored-By lines, one task = one commit minimum.
- **Verify honesty:** every gate command run by conductor, output pasted. Subagent claims are not evidence.

## File Structure

```
nabhasa/
├── .claude/specs/2026-07-05-nabhasa.md   (this file)
├── docs/
│   ├── art-direction.md                  (Task 0 — gates all visuals)
│   └── assets-ledger.md                  (per-asset license)
├── public/
│   ├── basis/ draco/                     (KTX2/Draco transcoders)
│   ├── fonts/                            (woff2)
│   └── audio/                            (IRs, loops — CC0/PD only)
├── src/
│   ├── main.tsx  App.tsx
│   ├── core/
│   │   ├── renderer.tsx      (Canvas + async WebGPURenderer factory)
│   │   ├── tiers.ts          (detectTier)
│   │   ├── quality.ts        (per-tier knobs: DPR, particle counts, post)
│   │   ├── loop.ts           (fixed-step accumulator)
│   │   └── perfProbe.ts      (fps harness → console table)
│   ├── world/
│   │   ├── scale.ts          (sim constants — single source of gameplay physics)
│   │   ├── NeutronStar.tsx   (surface shader + rotation)
│   │   ├── Starfield.tsx     (instanced sprites; feeds lensing cubemap)
│   │   └── regions.ts        (region graph + proximity streaming)
│   ├── flight/
│   │   ├── input.ts          (unified keyboard/touch/gamepad poll)
│   │   ├── gravity.ts        (inverse-square force)
│   │   ├── Craft.tsx         (Rapier body + ship visuals + thruster FX)
│   │   └── cameraRig.ts      (chase cam, exp damping)
│   ├── shaders/              (ALL TSL)
│   │   ├── lensing.ts        ★ FABLE-OWNED
│   │   ├── starSurface.ts  collapse.ts  pulsarBeam.ts  nebula.ts
│   ├── signatures/
│   │   ├── CollapsePreloader.tsx  LensingSkybox.tsx
│   │   ├── PulsarBeams.tsx        DysonSwarm.tsx
│   ├── game/
│   │   ├── courier.ts        (mission FSM + scoring)
│   │   ├── score.ts          (localStorage)
│   │   └── ResultCard.tsx    (shareable canvas card)
│   ├── hud/
│   │   ├── physics-data.ts   (REAL astrophysics formulas)
│   │   ├── hudStore.ts       (zustand, 10Hz throttle)
│   │   └── Telemetry.tsx     (diegetic HUD DOM overlay)
│   ├── audio/
│   │   ├── engine.ts         (context, gesture unlock, master graph)
│   │   ├── ambient.ts        (composed bed)
│   │   └── sonify.ts         (data layer: pulse/flare/roar)
│   ├── mobile/FlythroughFilm.tsx
│   └── fallback/StaticExperience.tsx
└── tests/  (vitest: gravity.test.ts, tiers.test.ts, score.test.ts, input.test.ts, courier.test.ts)
```

**World model (all tasks share this):** 1 world unit (wu) = 1 km visual scale. Star at origin, radius 10 wu. Sim gravity `GM_SIM = 270_000 wu³/s²` (circular orbit at r=300 → v=30 wu/s). Kill radius 25 wu (tidal destruction + respawn). Playable sphere r=3000 wu with soft push-back boundary. Dyson swarm region centered `(900, 0, 0)` radius 250 wu. Fixed physics step 1/60 s.

---

## WAVE 0 — Foundation (art direction, renderer, loop)

### Task 0: Art-direction doc (BLOCKS all visual tasks)

**Packet:** role=art director · scope=one doc · non-scope=any code · invariant=real references only, no AI-generated refs shipped as direction · gate=Praneesh sign-off.

**Files:** Create: `docs/art-direction.md`, `docs/assets-ledger.md` (empty table w/ header `| asset | source | license | attribution |`).

- [ ] **Step 1:** Write `docs/art-direction.md` containing, concretely: palette (void near-black base + ONE hot accent for the star/beams + cold dim UI tone — exact hex values chosen against reference stills); typography scale (Space Grotesk display / Inter body / JetBrains Mono telemetry, sizes + tracking); reference board — minimum 8 linked real references: Lusion.co, bruno-simon.com, OceanX 2025 (Unseen), Persepolis Reimagined, "Singularity" by MisterPrada (webgpu.com/showcase/singularity), NASA SVS neutron-star visualizations, Interstellar Gargantua stills, one obys.agency type reference; per-region mood (arrival / near-star / swarm) with lighting key (one key + one rim + ambient floor, photographic discipline); post-processing intent per region (what bloom is FOR each place, thresholds >1 emissive-only); imperfection layer spec (grain amount, chromatic aberration only at high-G moments, lens dirt never).
- [ ] **Step 2:** Praneesh reviews and approves. Record approval line + date at top of doc.
- [ ] **Step 3:** Commit: `docs: art direction (gates all visual work)`.

### Task 1: Repo bootstrap

**Packet:** role=builder · scope=tooling only · non-scope=any scene code · invariant=pinned majors · gate=`npm run build && npm test` green.

**Files:** Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `index.html`, `src/main.tsx`, `src/App.tsx`, `.gitignore`, `vitest.config.ts`.

- [ ] **Step 1:** Scaffold: `npm create vite@latest . -- --template react-ts`. Then install exact deps:
```bash
npm i three @react-three/fiber@^9.6 @react-three/drei@^10 zustand@^5 gsap@^3.13
npm i -D vitest @types/three
```
Do NOT install rapier yet (lazy, Task 6). Pin three to the current r18x at install time (≥0.185.0); record the exact version in this spec's revision log.
- [ ] **Step 2:** `vite.config.ts` — default React plugin; add `assetsInlineLimit: 0` (KTX2/wasm must never inline). `tsconfig`: `strict: true`.
- [ ] **Step 3:** `index.html` — `<title>Nabhasa</title>`, dark `background:#000` on `<html>` (no white flash), font preloads (added Task 13), `<meta name="description">` real copy.
- [ ] **Step 4:** Smoke test `tests/smoke.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
describe('toolchain', () => { it('runs', () => expect(1 + 1).toBe(2)); });
```
- [ ] **Step 5:** Run: `npm run build && npx vitest run` → expect both green. Paste output.
- [ ] **Step 6:** Commit: `chore: vite+react19+ts scaffold, pinned three/r3f stack`.

### Task 2: Renderer, tier detection, quality knobs, perf probe

**Packet:** role=builder · scope=`src/core/*` · non-scope=world content · invariant=one Canvas, one RAF, TSL-only imports · gate=tier tests green + app renders empty scene at 60fps all tiers.

**Files:** Create: `src/core/tiers.ts`, `src/core/quality.ts`, `src/core/renderer.tsx`, `src/core/perfProbe.ts`. Test: `tests/tiers.test.ts`.

**Interfaces (later tasks consume):**
- `detectTier(): Promise<Tier>` where `type Tier = 'webgpu-high' | 'webgpu-low' | 'webgl2' | 'static'`
- `QUALITY[tier]: { dpr: [number, number], collapseParticles: number, swarmTiles: number, lensing: 'full' | 'half' | 'off', post: boolean }`
- `<NabhasaCanvas tier={tier}>{children}</NabhasaCanvas>` — wraps R3F `<Canvas>`
- `usePerfProbe(label: string)` — hook; logs `{label, avgFps, p1Fps, frames}` table after 10s sampling; also `window.__perf` for gate capture.

- [ ] **Step 1:** Failing tests `tests/tiers.test.ts` — mock `navigator.gpu` absent → `'webgl2'`; present but `requestAdapter()` → null (blocklist trap) → `'webgl2'`; adapter ok + `prefers-reduced-motion: reduce` (mock `matchMedia`) → `'static'`; adapter ok + high `maxBufferSize` → `'webgpu-high'`; adapter ok + low limits or mobile UA → `'webgpu-low'`. Run: `npx vitest run tests/tiers.test.ts` → FAIL (module missing).
- [ ] **Step 2:** Implement `tiers.ts`:
```ts
export type Tier = 'webgpu-high' | 'webgpu-low' | 'webgl2' | 'static';

export async function detectTier(): Promise<Tier> {
  if (matchMedia('(prefers-reduced-motion: reduce)').matches) return 'static';
  if (!('gpu' in navigator)) return 'webgl2';
  const adapter = await (navigator as any).gpu.requestAdapter();
  if (!adapter) return 'webgl2'; // navigator.gpu present but blocklisted
  const coarse = matchMedia('(pointer: coarse)').matches;
  const big = adapter.limits.maxBufferSize >= 1 << 30; // 1GiB+
  return !coarse && big ? 'webgpu-high' : 'webgpu-low';
}
```
`quality.ts` (single source of every tier-dependent number — no scene hardcodes counts):
```ts
export const QUALITY = {
  'webgpu-high': { dpr: [1, 2],   collapseParticles: 1_000_000, swarmTiles: 150_000, lensing: 'full', post: true  },
  'webgpu-low':  { dpr: [1, 1.5], collapseParticles: 200_000,   swarmTiles: 40_000,  lensing: 'half', post: true  },
  'webgl2':      { dpr: [1, 1.5], collapseParticles: 50_000,    swarmTiles: 10_000,  lensing: 'half', post: false },
  'static':      { dpr: [1, 1],   collapseParticles: 0,         swarmTiles: 0,       lensing: 'off',  post: false },
} as const;
```
- [ ] **Step 3:** `renderer.tsx` — R3F v9 async `gl` factory:
```tsx
import * as THREE from 'three/webgpu';
import { Canvas, extend } from '@react-three/fiber';
import { QUALITY } from './quality';
import type { Tier } from './tiers';
extend(THREE as any);

export function NabhasaCanvas({ tier, children }: { tier: Tier; children: React.ReactNode }) {
  return (
    <Canvas
      dpr={QUALITY[tier].dpr as any}
      frameloop="always"
      gl={async (props) => {
        const renderer = new THREE.WebGPURenderer({ ...(props as any), antialias: true, forceWebGL: tier === 'webgl2' });
        await renderer.init(); // WebGPU probe + auto WebGL2 fallback
        renderer.toneMapping = THREE.AgXToneMapping;
        renderer.outputColorSpace = THREE.SRGBColorSpace;
        return renderer;
      }}
    >
      {children}
    </Canvas>
  );
}
```
- [ ] **Step 4:** `perfProbe.ts` — `useFrame` hook accumulating frame times for 10s → computes avg + 1% low, `console.table` + `window.__perf = {label, avgFps, p1Fps}`. ~25 lines, no deps (stats-gl is broken on WebGPURenderer r181+ — this replaces it).
- [ ] **Step 5:** Wire `App.tsx`: `detectTier()` → render `<NabhasaCanvas>` with a placeholder emissive sphere + probe. Run `npx vitest run` → PASS. Run dev, check console probe ≥60fps, and verify fallback: `?forceTier=webgl2` query-param override in `detectTier` (also add `?forceTier=` handling — needed by every later gate).
- [ ] **Step 6:** Commit: `feat(core): webgpu renderer w/ tier detect, quality table, perf probe`.

### Task 3: Fixed-step loop + unified input

**Packet:** role=builder · scope=`core/loop.ts`, `flight/input.ts` · non-scope=craft/physics · invariant=one input state object polled per frame, zero per-event React work · gate=input tests green.

**Files:** Create: `src/core/loop.ts`, `src/flight/input.ts`. Test: `tests/input.test.ts`.

**Interfaces:**
- `createFixedLoop(step: (dt: number) => void, hz = 60): { tick(frameDt: number): number }` — returns interpolation alpha; clamps frameDt to 1/30; zeroes accumulator after pause gaps >0.5s.
- `input: { thrust: number(-1..1), strafe: number, lift: number, pitch: number, yaw: number, roll: number, boost: boolean, brake: boolean, interact: boolean }` — module singleton, `attachInput(el): () => void`, `pollGamepad(): void` (called each frame).

- [ ] **Step 1:** Failing tests: loop accumulates (tick(0.05) at 60Hz → 3 steps); clamps huge dt (tick(2.0) → ≤ ceil(1/30*60)=2 steps, accumulator reset); keyboard keydown 'KeyW' sets `input.thrust=1`, keyup resets. Run → FAIL.
- [ ] **Step 2:** Implement both. Keyboard: track pressed-set on `keydown/keyup` (WASD+RF = translate, arrows/QE = rotate, Shift=boost, Space=brake, E=interact). Gamepad: `navigator.getGamepads()` poll, `mapping==='standard'` only, deadzone 0.15. Touch: two-zone (left = virtual stick → thrust/strafe, right = drag → pitch/yaw), deadzone, `touch-action: none`. Merge = last-writer-wins per axis; expose `input.lastSource: 'kbd'|'pad'|'touch'` for control hints.
- [ ] **Step 3:** `npx vitest run` → PASS. Commit: `feat: fixed-step loop + unified kbd/touch/gamepad input`.

---

## WAVE 1 — World + flight (the core loop must feel good before anything else)

### Task 4: World scale + real-physics data module

**Packet:** role=builder · scope=two pure-data modules · non-scope=rendering · invariant=sim and real constants NEVER cross-import · gate=physics tests green vs hand-checked values.

**Files:** Create: `src/world/scale.ts`, `src/hud/physics-data.ts`. Test: `tests/physics-data.test.ts`.

**Interfaces:**
- `scale.ts` exports: `GM_SIM = 270_000`, `STAR_RADIUS = 10`, `KILL_RADIUS = 25`, `PLAY_RADIUS = 3000`, `SWARM_CENTER = [900,0,0]`, `SWARM_RADIUS = 250`, `FIXED_DT = 1/60`, `SPIN_DISPLAY_SLOWDOWN = 4`.
- `physics-data.ts` exports (real SI, M = 1.4 M☉, R = 10 km, P = 0.71452 s): `PULSAR = { name: 'PSR B0329+54', periodS, massKg, radiusM, schwarzschildM }`, `timeDilation(rKm): number` = `Math.sqrt(1 - r_s/r)`, `tidalAccel(rKm, spanM): number` = `2·GM·span/r³`, `orbitalV(rKm)`, `surfaceGravity()`. 1 wu = 1 km is the display mapping.

- [ ] **Step 1:** Failing tests with hand-computed values: `schwarzschildM ≈ 4134` (±1%); `timeDilation(10) ≈ 0.766` (±0.005); `timeDilation(300) ≈ 0.9931`; `tidalAccel(300, 2) ≈ 2.75e4 m/s²` (±2%); `surfaceGravity() ≈ 1.86e12 m/s²` (±2%). Run → FAIL.
- [ ] **Step 2:** Implement with real constants (G = 6.674e-11, M☉ = 1.989e30, c = 2.998e8). Run → PASS. These numbers ARE the site's content — Praneesh physics-reviews this file.
- [ ] **Step 3:** Commit: `feat(world): sim scale constants + real PSR B0329+54 physics module`.

### Task 5: Neutron star + starfield + region graph

**Packet:** role=builder(visual) · scope=`world/*` render components, `shaders/starSurface.ts`, `shaders/nebula.ts` · non-scope=lensing, beams · invariant=art-direction palette only; starfield renders into a cubemap consumable by lensing · gate=60fps probe + Lusion-check screenshot review.

**Files:** Create: `src/world/NeutronStar.tsx`, `src/world/Starfield.tsx`, `src/world/regions.ts`, `src/shaders/starSurface.ts`, `src/shaders/nebula.ts`.

**Interfaces:**
- `<NeutronStar />` — sphere r=10 at origin; TSL surface: blackbody-hot emissive (>1 for selective bloom), magnetic-pole hotspots, rotation at `2π/(0.71452·4)` rad/s around +Y tilted 15°. Exposes `starSpinAngle(t): number` (pure fn, shared with beams + sonify so all three phase-lock).
- `<Starfield tier={tier} />` — instanced point-sprites (one draw call), 30k stars + TSL FBM nebula backplate; **also renders once into a `CubeTexture` (512³, `renderTarget`)** exported as `getStarfieldCube(): THREE.CubeTexture` — the lensing shader's input.
- `regions.ts`: `type RegionId = 'arrival' | 'nearStar' | 'swarm'`; `regionAt(pos: Vector3): RegionId`; `useRegion()` zustand selector; region boundaries: nearStar = r<150, swarm = within SWARM_RADIUS of SWARM_CENTER, else arrival. Each region carries `{ exposure: number, ambientLevel: number }` consumed by post + audio.

- [ ] **Step 1:** Starfield: `InstancedMesh` sprites, position = uniform sphere shell r∈[2500,6000], size/temp variation, TSL twinkle (`time`-driven sin hash). Nebula: fragment TSL, 5-octave FBM + domain warp (IQ pattern), rendered on inward-facing far sphere, dim (never competes with star). Cubemap bake once at init.
- [ ] **Step 2:** Star surface TSL: emissive blackbody ramp (temp uniform), polar hotspots aligned to beam axis, subtle surface noise scroll. Emissive intensity 3–5 (bloom threshold >1 catches only this).
- [ ] **Step 3:** `regions.ts` pure functions + store wiring. Unit test `regionAt` boundaries in `tests/regions.test.ts` (3 asserts).
- [ ] **Step 4:** Gate: perf probe on all 3 live tiers (`?forceTier=`), paste table; screenshot vs art-direction palette; commit `feat(world): star, starfield+nebula cubemap, region graph`.

### Task 6: Craft flight — Rapier body, gravity, chase camera (THE FEEL TASK)

**Packet:** role=builder(gameplay) · scope=`flight/*` · non-scope=missions/scoring · invariant=flight must be assessed fun by Praneesh before Wave 2 merges — this is the product · gate=orbit-stability test + manual feel session.

**Files:** Create: `src/flight/gravity.ts`, `src/flight/Craft.tsx`, `src/flight/cameraRig.ts`. Test: `tests/gravity.test.ts`. Modify: `package.json` (add `@react-three/rapier@^2.2`).

**Interfaces:**
- `gravityAccel(pos: Vector3, out: Vector3): Vector3` — `a = -GM_SIM · r̂ / max(r², KILL_RADIUS²)` (clamped: no singularity).
- `<Craft />` — Rapier `RigidBody` (ccd, linearDamping 0, angularDamping 2). Per fixed step: apply gravity force, thrust from `input` (max 40 wu/s², boost ×2.5), flight-assist retro-damping 0.3 when `input.brake`. Kill: `r < KILL_RADIUS` → respawn at `(600, 80, 0)` facing star, GSAP 600ms white-in fade + tidal-destruction audio cue. Soft boundary: beyond PLAY_RADIUS, inward spring force. Exposes `useCraftState(): { pos, vel, speed, fuel }` (refs, not state).
- `cameraRig.ts`: chase camera — target = craft pos − heading·28 + up·8; damping `k = 1 − exp(−λ·dt)`, λ=4 position / 6 look-at; FOV 60→75 lerp with speed (speed lines of the poor); never auto-rotates against input.
- Rapier lazy: `const Physics = React.lazy(...)` — Rapier WASM must not block first paint (preloader is already up).

- [ ] **Step 1:** Failing test `tests/gravity.test.ts` — pure integrator harness (no Rapier in test; semi-implicit Euler at 60Hz mirroring Rapier's step): circular orbit r=300, v=(0,0,30): after 3600 steps (60s), radius ∈ [285, 315] and speed ∈ [27, 33]. Also `gravityAccel` at r=300 → |a| = 3.0 wu/s² (±1e-6); clamp check at r=1 → uses KILL_RADIUS². Run → FAIL, implement `gravity.ts`, → PASS.
- [ ] **Step 2:** Craft + camera per interfaces. Thruster visuals: small emissive cone quads keyed to thrust axis (placeholder mesh OK this wave; hero glTF in Task 13).
- [ ] **Step 3:** Fixed-step wiring: Rapier `timeStep={FIXED_DT}` `updateLoop="independent"`; gravity applied via `addForce` each step, reset each step.
- [ ] **Step 4:** Gate: vitest green (paste); 10-minute manual fly session by Praneesh — verdict logged in commit body ("feel: approved" or iterate); probe ≥60fps with physics live. Commit: `feat(flight): rapier craft, clamped inverse-square gravity, chase rig`.

---

## WAVE 2 — Signatures I (lensing ★, collapse preloader)

### Task 7: Gravitational-lensing skybox ★ FABLE-OWNED

**Packet:** role=Fable builds prototype pre-2026-07-07; GLM integrates · scope=`shaders/lensing.ts`, `signatures/LensingSkybox.tsx` · non-scope=star surface, beams · invariant=single TSL graph runs WGSL AND GLSL; `lensing: 'half'` tier renders half-res + bilateral upscale · gate=Einstein ring visually correct (ring radius grows as camera nears), 60fps high / 30fps webgl2.

**Files:** Create: `src/shaders/lensing.ts`, `src/signatures/LensingSkybox.tsx`. Consumes `getStarfieldCube()` from Task 5.

**Approach (locked):** fullscreen fragment pass (NOT compute — fragment TSL auto-transpiles to GLSL for the webgl2 tier). Per-pixel: ray from camera; impact parameter `b` vs star at origin; thin-lens deflection `α = 2·r_s_vis/b` (visual r_s tuned ~6 wu; physical formula `4GM/c²b` shape, magnitude art-directed); bend ray toward star by α; sample `starfieldCube` along bent ray. `b < photonRing (≈15 wu)` → capture: sample falls to black with thin bright ring at the boundary (the Einstein ring). Doppler tint: subtle blue/red shift by tangential component sign (from `vlwkaos/threejs-blackhole` reference). Half-res tier: render to ½-res target, bilateral upscale node.

**References (in vault research):** threejsroadmap.com "Raytracing a Black Hole with WebGPU" (TSL walkthrough), vlwkaos/threejs-blackhole, oseiskar/black-hole, MisterPrada "Singularity".

- [ ] **Step 1 (Fable):** standalone prototype page `/dev/lensing` — skybox pass + orbit camera, uniforms: `starPos`, `rsVis`, `photonRing`, `ringIntensity`, leva-tunable.
- [ ] **Step 2 (Fable):** verify both backends: `?forceTier=webgl2` renders identical composition (screenshot diff by eye), no shader compile errors in console. Paste probe both tiers.
- [ ] **Step 3 (GLM):** integrate behind craft camera as the permanent sky (always-on ambient per Final Plan), remove starfield direct render on tiers where lensing owns the sky (avoid double sky).
- [ ] **Step 4:** Gate review vs reference stills; commit `feat(signature): gravitational lensing skybox (TSL, dual-backend)`.

### Task 8: Collapse preloader (signature 1)

**Packet:** role=builder(visual) · scope=`signatures/CollapsePreloader.tsx`, `shaders/collapse.ts`, asset-loading plumbing · non-scope=audio graph internals (consumes Task 10 API if merged, else silent) · invariant=loader is DIEGETIC — progress = collapse; never a progress bar · gate=full sequence <threshold jank on throttled network, works all live tiers.

**Files:** Create: `src/signatures/CollapsePreloader.tsx`, `src/shaders/collapse.ts`.

**Interfaces:** `<CollapsePreloader onEnter={(sound: boolean) => void} />` — full-screen; drives real asset loading via `THREE.DefaultLoadingManager` progress → uniform `progress ∈ [0,1]`.

**Sequence (locked, concept #1):** giant star full-frame (TSL: FBM turbulent red-supergiant surface). As `progress` rises: radius 1→0.15, color temp 4500K→1800K, turbulence rises, gravity-darkening at limb. At `progress=1` + assets settled: 300ms implosion (radial inward particle burst — `QUALITY[tier].collapseParticles` via compute `instancedArray` on WebGPU / 50k sprite fallback), 120ms black, neutron star snaps in with expanding shockwave ring (torus + TSL falloff) → camera pulls back to reveal craft → **"ENGAGE — sound on" / "enter silent"** buttons (this click = the WebAudio unlock gesture). Audio (if Task 10 merged): descending sub-drone → click → reverb bloom.

- [ ] **Step 1:** Build progress-driven star (leva `progress` slider to art-direct before wiring real loading).
- [ ] **Step 2:** Particle implosion per tier; compute path writes positions in `instancedArray`, fallback = CPU-seeded sprites with shader-side inward drift.
- [ ] **Step 3:** Wire real loading manager; test with DevTools "Slow 4G" — sequence must degrade to slower collapse, never stall at fake 99%.
- [ ] **Step 4:** Gate: probe during particle burst ≥60/30fps; visual review; commit `feat(signature): stellar-collapse preloader`.

---

## WAVE 3 — Sound + pulsar (the heartbeat)

### Task 9: Audio engine — ambient bed + sonification layer

**Packet:** role=builder(audio) · scope=`audio/*` · non-scope=visuals · invariant=mute-by-default until gesture; sonification = real data mapping (muting genuinely loses info); zero audio files for synth paths · gate=A/B listen session + zero-gesture autoplay errors in console.

**Files:** Create: `src/audio/engine.ts`, `src/audio/ambient.ts`, `src/audio/sonify.ts`.

**Interfaces:**
- `engine.ts`: `initAudio(): void` (must be called from user gesture — the ENGAGE click), `setMuted(b)`, master `GainNode` → `DynamicsCompressorNode` → destination; suspend on `visibilitychange`.
- `ambient.ts`: `startAmbient()` — composed bed: 3 detuned low sine/triangle oscillators (55Hz ± 0.7Hz beat), lowpass 400Hz, slow LFO on gain (0.05Hz), `ConvolverNode` with small IR (CC0, ≤200KB) for space. ~0KB payload ex-IR.
- `sonify.ts`: `updateSonify(state: { rKm: number, beamPhase: number, flare: boolean })` called from the fixed loop at 10Hz:
  - **rotation→pulse:** each beam crossing of camera azimuth (phase from `starSpinAngle`, display rate) → ping: sine sweep 880→220Hz over 90ms through bandpass, gain ∝ beam-camera alignment. The site's heartbeat, phase-locked to the visual sweep.
  - **accretion→roar:** looped brown-noise (generated `AudioBuffer`, 2s, seamless) through lowpass; gain + cutoff ∝ `1/r` (audible descent as you dive).
  - **flares→hits:** on flare events (Task 11 emits): noise burst through resonant filter, 200ms decay.

- [ ] **Step 1:** Engine + unlock flow; assert console clean of "AudioContext was not allowed" on load (no node created pre-gesture).
- [ ] **Step 2:** Ambient bed; tune against art-direction mood note. **Step 3:** Sonification mappings wired to real craft state.
- [ ] **Step 4:** Gate: listen session (Praneesh) — "sound off feels like information loss" must be true; commit `feat(audio): ambient bed + live star sonification`.

### Task 10: Pulsar beams (signature 3)

**Packet:** role=builder(visual) · scope=`signatures/PulsarBeams.tsx`, `shaders/pulsarBeam.ts` · non-scope=audio internals (emits phase to sonify) · invariant=beam phase = `starSpinAngle` (single source, visual/audio/HUD phase-lock); flash events <3Hz (photosensitivity) · gate=fly-through moment review + probe.

**Files:** Create: `src/signatures/PulsarBeams.tsx`, `src/shaders/pulsarBeam.ts`.

**Behavior (locked, concept #4):** twin opposed volumetric cones from magnetic poles (15° off spin axis — real pulsar geometry, gives the sweep its wobble), rotating at display rate (1:4 real, = 0.35 rev/s → 0.7 beam-passes/s, comfortably <3Hz). TSL cone: additive, radial falloff, noise-modulated density, length ~2000 wu. **Flying through a beam:** screen-space bloom lift + local craft rim-light + HUD "RADIATION TRANSIT" readout + sonify ping at max gain — the moment must feel physical. Local lighting: one `PointLight` snapped to nearest beam axis point, intensity ∝ alignment.

- [ ] **Step 1:** Beam cones w/ TSL falloff shader, phase from `starSpinAngle`. **Step 2:** transit detection (point-to-cone distance from craft), drive light + HUD + `updateSonify.beamPhase`. **Step 3:** luminance-delta cap: beam transit raises exposure ≤1.8× over ≤400ms (no strobe).
- [ ] **Step 4:** Gate: probe; fly-through review ("physical" verdict); commit `feat(signature): pulsar lighthouse beams, phase-locked to sonification`.

---

## WAVE 4 — Swarm, game, HUD (the payoff)

### Task 11: Dyson swarm region (signature 4)

**Packet:** role=builder(visual+compute) · scope=`signatures/DysonSwarm.tsx` region · non-scope=missions · invariant=assembly driven by craft proximity (spatial discovery, NOT timeline); tile counts from QUALITY only · gate=approach sequence review + probe in-region.

**Files:** Create: `src/signatures/DysonSwarm.tsx`. Consumes `regions.ts`, emits `flare` events to sonify.

**Behavior (locked, concept #8):** `QUALITY[tier].swarmTiles` instanced mirror-tiles in 3 orbital shells around SWARM_CENTER structure point (a partial Dyson node under construction — not enclosing the star; scale honesty). Assembly parameter `a ∈ [0,1]` = smoothstep of craft distance (from 2×SWARM_RADIUS → 0.3×): tiles lerp scattered-cloud → orbital slot with per-tile stagger hash; slotted tiles catch starlight (TSL: mirror BRDF flash as their normal aligns — the "glitter wave" as assembly completes). WebGPU: compute node writes instance matrices (`instancedArray`, r182 StorageInstancedBufferAttribute path). WebGL2: CPU chunked update (≤2ms/frame budget, 10k tiles). Occasional specular "flare" event (glint alignment) → sonify hit.

- [ ] **Step 1:** Instanced tiles + slot math (3 shells, golden-angle distribution). **Step 2:** proximity-driven `a` + stagger lerp; compute path, then CPU path. **Step 3:** glint BRDF + flare events.
- [ ] **Step 4:** Gate: probe in-region all tiers (paste); approach-flight review ("Act-3 payoff" verdict); commit `feat(signature): dyson swarm assembly region`.

### Task 12: Slingshot-courier game + score

**Packet:** role=builder(gameplay) · scope=`game/*` · non-scope=new physics (uses flight verbatim — game IS navigation) · invariant=no backend; score localStorage; missions never lock free flight (declinable, abandonable) · gate=courier tests green + full mission playthrough.

**Files:** Create: `src/game/courier.ts`, `src/game/score.ts`, `src/game/ResultCard.tsx`. Test: `tests/courier.test.ts`, `tests/score.test.ts`.

**Interfaces:**
- `courier.ts`: FSM `idle → offered (near beacon) → active → delivered | failed(fuel|destroyed) → idle`; `type Mission = { id, from: Vec3, to: Vec3, fuelBudget: number, par: number }`; 5 authored missions (JSON in-file), escalating: straight run → single assist → tight periapsis assist → beam-transit route → swarm-threading finale. Fuel: thrust drains `fuel -= thrust·dt·rate` only while mission active; assists (thrust-free Δv) are the score lever. `score = max(0, round(1000·fuelFrac + 500·max(0, par/time)))`.
- `score.ts`: `loadScores(): Scores`, `saveScore(missionId, score)` — localStorage key `nabhasa.scores.v1`, try/catch (private mode), schema-validated parse, corrupt → reset.
- `ResultCard.tsx`: on delivery — offscreen `<canvas>` render (star + trajectory trace + score + mission name, art-direction typography) → PNG via `toBlob` → `navigator.share` if available else download link.

- [ ] **Step 1:** Failing tests: FSM legal/illegal transitions (delivered only from active; abandon → idle); score formula fixture (`fuelFrac=0.5, time=par` → 1000); localStorage round-trip + corrupt-JSON reset. Run → FAIL.
- [ ] **Step 2:** Implement; beacons = small diegetic structures with light pillar (visible from distance — wayfinding without map UI).
- [ ] **Step 3:** vitest → PASS (paste). Full playthrough of all 5 missions by Praneesh. Commit: `feat(game): slingshot courier missions, local scores, shareable result card`.

### Task 13: Diegetic HUD + craft hero model + polish details

**Packet:** role=builder(UI) · scope=`hud/*`, ship glTF, cursor/404 · non-scope=new telemetry physics (consumes Task 4) · invariant=HUD values from `physics-data.ts` REAL formulas; JetBrains Mono; DOM overlay (crisp text, a11y-readable), not in-canvas sprites · gate=physics numbers hand-checked + HUD review.

**Files:** Create: `src/hud/hudStore.ts`, `src/hud/Telemetry.tsx`, `public/models/craft.glb`, `src/pages/404.html`, custom cursor CSS. Modify: `src/flight/Craft.tsx` (swap placeholder mesh → glb).

- [ ] **Step 1:** `hudStore.ts` — zustand, written from fixed loop at 10Hz: `{ rKm, speed, dilation, tidalG, fuel, region, mission, beamTransit }` (dilation/tidal via `physics-data.ts` with rKm = r_wu).
- [ ] **Step 2:** `Telemetry.tsx` — corner clusters, JetBrains Mono, values as instrumentation (`TIDAL Δa 2.7e4 m/s² · SPIN 0.71452s (display 1:4) · t_surface/t_you 0.9931`). Legibility: subtle backdrop, WCAG AA contrast. Control hints keyed to `input.lastSource`.
- [ ] **Step 3:** Craft hero model: NASA 3D Resources base or Blender-authored, → glTF+meshopt+KTX2 via `gltf-transform` (≤500KB); ledger entry with license.
- [ ] **Step 4:** Details pass: custom cursor (dot + soft ring, thrust-reactive), crafted 404 (drifting derelict tile + one line + link home), favicon, social OG card.
- [ ] **Step 5:** Gate: Praneesh checks 3 HUD numbers against `physics-data.test.ts` fixtures; visual review; commit `feat(hud): diegetic real-physics telemetry + hero craft + detail polish`.

---

## WAVE 5 — Platform floor + ship

### Task 14: Mobile linear cut (designed-down film)

**Packet:** role=builder · scope=`mobile/FlythroughFilm.tsx` + routing in App · non-scope=touch-flight (explicitly cut) · invariant=same sound, same reveals, no Rapier/game load on mobile; never broken parity · gate=mid-range Android real-device run ≥30fps.

**Behavior (locked):** `pointer: coarse` + width <900px → film mode: camera on authored Catmull-Rom spline visiting all four set-pieces (collapse → lensing orbit → beam transit → swarm assembly), progress scrubbed by touch-drag inertia (GSAP timeline, not scroll), sonification live, HUD condensed to one readout line, DPR ≤1.5, `webgpu-low`-or-below quality caps. Rapier + game chunks never fetched (verify in network tab — paste request list).

- [ ] Steps: spline + scrub → tier caps → device test (Praneesh's phone) → commit `feat(mobile): designed-down fly-through film`.

### Task 15: Reduced-motion static experience + a11y floor

**Packet:** role=builder · scope=`fallback/StaticExperience.tsx` · non-scope=3D · invariant=real content parity (all telemetry facts readable as text); this doubles as no-WebGL2 fallback · gate=axe-core clean + `prefers-reduced-motion` E2E check.

- [ ] **Step 1:** `tier === 'static'` (or renderer init failure) → static page: 3–4 captured hero stills (from the real build, committed as compressed AVIF ≤150KB each), full pulsar data table, mission-log prose describing the four moments, same typography. No autoplaying motion, no canvas.
- [ ] **Step 2:** A11y floor sitewide: focus-visible styles, ENGAGE buttons keyboard-reachable, HUD `aria-live="off"` (updates too frequent) with a static "system status" summary region, `alt` on stills.
- [ ] **Step 3:** Gate: `npx @axe-core/cli` on static page → 0 violations (paste); OS reduced-motion toggle manual check. Commit: `feat(fallback): static experience + a11y floor`.

### Task 16: Performance hardening + deploy

**Packet:** role=builder(perf) · scope=budgets, chunking, Vercel · non-scope=new features (freeze) · invariant=budgets from Global Constraints are gates, not goals · gate=all numbers pasted in commit body.

- [ ] **Step 1:** Chunk audit: `npx vite-bundle-visualizer` — main ≤500KB gz; rapier+game lazy chunk; mobile film chunk separate. Paste sizes.
- [ ] **Step 2:** Asset audit: total initial ≤8MB on cold load (DevTools network, paste); KTX2 everywhere; fonts subset.
- [ ] **Step 3:** Probe matrix: paste `__perf` for {webgpu-high, webgpu-low, webgl2} × {near-star+beams, swarm, collapse}. All ≥60fps (webgl2 ≥30).
- [ ] **Step 4:** Vercel: project + `vercel.json` (immutable cache headers for `/assets`, `/basis`, `/draco`), deploy preview, run full experience on deployed URL. Domain: OPEN — ask Praneesh at this step (external gate).
- [ ] **Step 5:** Footer credits: ESA CC-BY attributions from `assets-ledger.md`, "PSR B0329+54 data" source line. Commit: `chore: perf hardening + vercel deploy`.

---

## Done bar (verify against THIS, not against what got built)

1. I can fly freely from spawn and *discover* all four signature moments spatially — no scroll, no forced timeline. ✓ = fresh-eyes tester finds ≥3 of 4 unprompted.
2. Lensing skybox is always-on and reads as "how did they do that" (Einstein ring visible when star occludes starfield). Dual-backend.
3. Sound off = measurable information loss (beam phase, proximity, flares inaudible).
4. Courier missions playable start-to-finish, score persists, result card shares.
5. HUD physics numbers verified against `physics-data` tests by Praneesh.
6. All four tiers ship: probe matrix green, static page axe-clean, mobile film ≥30fps on real device.
7. Every anti-slop constitution line checked (loader/cursor/404/type/sound/mobile/"Lusion" review per scene).
8. Budgets: ≤8MB initial, main JS ≤500KB gz. Deployed on Vercel.

## Amendment A1 (2026-07-05, user request): Lich planet system

Eye-candy + content addition: the system is **PSR B1257+12 "Lich"** — the first
exoplanets ever discovered (Draugr / Poltergeist / Phobetor, real data, [VERIFY]
at physics review). Prototype landed in `src/world/planets.ts` + `/dev/system`
(commit 27bd445): procedural TSL PBR (fractal albedo, worley craters, noise
displacement, ember irradiation rim), Kepler-ratio compressed orbits, star as
sole point light. Integration rules: planets join `gravityAccel` as bodies and
serve as courier waypoints (Task 12 missions route between them) — they carry
gameplay, not just decoration. HUD shows real masses/periods (Task 4/13).
**DECIDED 2026-07-06: SWAP APPROVED** — system modeled on B1257+12 systemwide.
Global Constraints updated (P = 6.219 ms, display rate 1:2000, real ~161Hz
sonification tone). Task 4 `hud/physics-data.ts` + Task 9 sonify + Task 10
beam constants all use B1257+12; planets integrate as gravity bodies + courier
waypoints per this amendment.

## Revision log
- 2026-07-06 — Task 0 DONE: `docs/art-direction.md` written + Praneesh sign-off recorded (palette: void #030407 / star-hot #AFE3FF single accent / ui-cold #8A93A6 / irradiated #C46A4A material-only); `docs/assets-ledger.md` seeded. Star-swap DECIDED: B1257+12 systemwide (Amendment A1 + Global Constraints updated, display rate 1:2000). Tasks 2+3 dispatched to GLM-5.2 (packet: scoped builder, TDD, gates `npm run build && npx vitest run`).
- 2026-07-05 (later) — Task 1 DONE (commit 186a39d, three@0.185.1 pinned, build+vitest green; GLM scaffold, conductor ran gates). Task 7 steps 1–2 DONE (commit 78ce9d2): `/?dev=lensing` prototype, dual-backend verified (WGSL+GLSL identical), 60fps WebGL2/Intel Gen12; WebGPU fps gate PENDING discrete-GPU hardware (5fps here = Dawn/Mesa present-path issue, raw rAF 59fps, same graph 60fps on GLSL). Known TSL typing gotcha: `Fn` args need tuple annotation `([p]: [any])`, plain `any` matches the 0-arg builder overload. R3F gotcha: `gl` async factory must be referentially stable or renderer re-inits per render.
- 2026-07-05 — v1, written by Fable per Final Plan (locked 2026-07-05). Framework decision: Vite+React19 (not Next — single experience, no content routes; Plan B + mini-game research concur). Rapier retained per locked plan; gravity = custom clamped inverse-square force on the Rapier body (research's integrator warning honored via fixed-step + orbit-stability test).
